# Studio interface context

Operational developer workspace for configuring D20DAO lootbox and NFT reveal integration starters. The approved nine images in design/concepts guide visual composition, not implemented feature claims.

Use the actual D20DAO brand mark, black canvas, white text, Courier New navigation and labels, Arial body, coral actions, yellow focus and links, thin neutral rules and small control corners. Keep project lists and item tables as rows. No marketing hero, business metrics, wallets or deployment controls.

The workspace edits local drafts, previews generated files, compiles actual Solidity in a worker and exports boilerplate plus agent instructions. Numeric editing allows empty interim strings. Invalid configuration and incomplete onchain feature implementation remain explicit. Corrupt local storage must not be overwritten automatically. Small screens stack the sidebar, form and summary while preserving access to files and actions.
